//printing numbers 1 to 10 using Do While loop
#include <stdio.h>

int main () {
    int i=1;

    \\Do While lopp will runs one once weather condition are true or false or it's just like a normal while loop.
    
        do {
        
        printf("\n%d", i);
        i++;
        
     }while (i < 0); 
    
    return 0;
}
